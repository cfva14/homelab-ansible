- name: Check homelab servers
  hosts: servers
  gather_facts: false

  tasks:
    - name: Show hostname
      ansible.builtin.command: hostname
      register: hostname_result
      changed_when: false

    - name: Show hostname result
      ansible.builtin.debug:
        var: hostname_result.stdout